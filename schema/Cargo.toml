[package]
name = "vorpal-schema"
version = "0.1.0"
edition = "2021"

[dependencies]
prost = "0"
serde = { features = ["serde_derive"], version = "1" }
tonic = "0"

[build-dependencies]
tonic-build = "0"
