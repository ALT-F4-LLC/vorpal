name: dev

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - run: just format
        shell: $GITHUB_WORKSPACE/dev.sh

      - run: just lint
        shell: $GITHUB_WORKSPACE/dev.sh

      - run: just check
        shell: $GITHUB_WORKSPACE/dev.sh

      - run: just test
        shell: $GITHUB_WORKSPACE/dev.sh

      - run: just build
        shell: $GITHUB_WORKSPACE/dev.sh
