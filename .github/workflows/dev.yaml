name: dev

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - run: just format
        shell: ./dev.sh

      - run: just lint
        shell: ./dev.sh

      - run: just check
        shell: ./dev.sh

      - run: just test
        shell: ./dev.sh

      - run: just build
        shell: ./dev.sh
