use_dev() {
    PATH_add "$HOME/.cargo/bin"
    PATH_add "$PWD/.env/bin"
    PATH_add "$PWD/script/bin"
    export NICKEL_IMPORT_PATH="$PWD/.vorpal/packages:$PWD"
    "${PWD}/script/dev.sh"
}

use dev
